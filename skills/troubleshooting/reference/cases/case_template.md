# Case: [简短描述]

<!--
此模板用于沉淀问题排查案例。
AI 在排查问题时会搜索 symptoms.keywords 和 symptoms.error_pattern 进行匹配。
请确保这两个字段包含足够的关键信息。
-->

## 元数据

| 字段 | 值 |
|------|-----|
| **状态** | Active / Deprecated / Archived |
| **日期** | YYYY-MM-DD |
| **最后验证日期** | YYYY-MM-DD |
| **作者** | @xxx |
| **模块** | [模块名] |
| **严重程度** | Critical / High / Medium / Low |
| **发现版本** | X.Y.Z |
| **修复 Commit** | [commit_id](链接) |

## 标签 (Tags)

```
模块: [module-name]
组件: [component-name]
错误类型: [crash/timeout/deadlock/memory_leak/data_corruption/...]
环境: [dev/test/ci/prod]
```

---

## 症状 (Symptoms)

### 关键词 (用于搜索匹配)

```
[错误码1] [错误码2] [异常类型] [模块名] [关键日志片段]
```

**示例**：`CONNECTION_TIMEOUT Database connection pool exhausted`

### 错误模式 (正则表达式)

```regex
[可用于自动匹配的正则表达式]
```

**示例**：`Connection \d+ timeout after \d+ms`

### 现象描述

- **用户视角**：[用户观察到的现象，如请求超时、报错信息]
- **系统视角**：[监控/日志中的异常表现]

### 典型错误日志

```
[粘贴典型的错误日志片段，保留关键信息，脱敏敏感数据]
```

---

## 触发条件 (Trigger Conditions)

| 条件 | 描述 |
|------|------|
| **环境** | [生产/测试/开发] |
| **版本** | [受影响的版本范围] |
| **配置** | [特定配置要求] |
| **负载** | [触发时的负载特征] |
| **前置操作** | [触发问题的操作序列] |

---

## 根因分析 (Root Cause)

### 直接原因

[问题的直接技术原因，一两句话说清楚]

### 深层原因

[可选：导致问题的架构/设计/代码层面的深层原因]

### 代码定位

```
文件: [相关文件路径]
函数: [相关函数名]
行号: [如适用]
```

---

## 诊断步骤 (Diagnosis)

### 1. 确认症状

```bash
# 执行此命令确认是否为该问题
[诊断命令]
```

**预期输出**：[如果是该问题，应该看到什么]

### 2. 收集信息

```bash
# 收集更多诊断信息
[信息收集命令]
```

### 3. 验证根因

```bash
# 验证是否为该根因
[验证命令]
```

**确认标志**：[满足什么条件可以确认是该问题]

---

## 解决方案 (Solution)

### 临时修复 (Temporary Fix)

**目的**：快速止血，恢复服务

```bash
# 临时修复命令
[命令]
```

**注意事项**：[临时修复的局限性和风险]

### 永久修复 (Permanent Fix)

**修复方案**：[描述根本解决方案]

**代码变更**：
- PR/Commit: [链接或 ID]
- 修复版本: [版本号]

**验证方法**：[如何验证修复生效]

---

## 规避措施 (Prevention)

### 监控告警

- **新增指标**：[建议新增的监控指标]
- **告警规则**：[建议的告警阈值]

### 代码/配置

- [防止问题复发的代码或配置建议]

### 运维规范

- [相关的运维注意事项]

---

## 相关链接

- **事故单**：[链接]
- **复盘文档**：[链接]
- **修复 PR**：[链接]
- **相关案例**：[链接到类似案例]

---

## Changelog

| 日期 | 变更 | 作者 |
|------|------|------|
| YYYY-MM-DD | 初始版本 | @xxx |
