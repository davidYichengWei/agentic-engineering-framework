---
description: 强制在处理任何任务前检查并使用相关技能
alwaysApply: true
enabled: true
---

# Skill 使用规则

**核心规则**：收到任务时，只要有 1% 的可能性存在适用的 skill，就必须加载它。这不是建议，是强制。

- 不要假设任务"太简单"而跳过 skill 检查
- 不要凭记忆执行 skill 内容——skill 会更新，每次都必须重新读取
